<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  <h1>
    <%= title %>
  </h1>
  <p>Welcome to <%= title %>
  </p>
  <h2>Listagem</h2>
  <ul>
    <% for (let i=0; i < result.length; i++) { %>
      <li>
        <%= result[i].name %> (<%= result[i].date %>) (<%= result[i].classe %>) (<%= result[i].taxi %>) (<%= result[i]._id %>)
      </li>
      <% } %>
  </ul>
  <h2>Cadastro</h2>
  <form action="/save" method="POST">
    <p>
      <label> Nome do cliente:
        <input type="text" name="name">
      </label>
    </p>
    <p>
      <label> Data do agendamento:
        <input type="date" name="date">
      </label>
    </p>
    <p>
      <label> Classe do taxi:
        <select name="classe" onchange="this.form.submit()" id=""></select>
        <option value="">Selecione uma classe</option>
      </label>
    </p>
    <p>
      <label> Táxis disponíveis:
        <select name="taxi" onchange="this.form.submit()" id=""></select>
        <option value="">Selecione um taxi</option>
      </label>
    </p>
    <p>
      <input type="submit" value="Salvar">
    </p>
  </form>
  <h2>Deletar agendamento</h2>
  <form action="/delete" method="POST">
    <p>
      <label> ID do agendamento a ser excluido:
        <input type="text" name="id">
      </label>
    </p>
    <p>
      <input type="submit" value="Excluir">
    </p>
  </form>

</body>

</html>